# HealthPH+ Backend / Frontend Structure

The project now has a deployment-ready backend scaffold and a frontend placeholder folder.

```text
HealthPH_Plus/
  backend/
    app/
      api/
      core/
      schemas/
      services/
      main.py
    requirements.txt
    Dockerfile
    README.md

  frontend/
    README.md

  lib/
  assets/
  android/
  ios/
  web/
  pubspec.yaml
```

## Important

The Flutter project remains at the root for now to avoid breaking the current mobile development setup.

The backend is ready to push to GitHub and deploy independently.

